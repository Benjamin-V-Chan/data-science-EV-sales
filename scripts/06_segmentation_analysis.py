# Load feature_data.csv from data/processed
# Select numeric features (Battery_Capacity_kWh, Discount_Percentage, Units_Sold)
# Standardize features
# Fit KMeans clustering (e.g., n_clusters=4)
# Assign cluster labels to DataFrame
# Save DataFrame with cluster labels to outputs/customer_segments.csv
# Print cluster centroids
